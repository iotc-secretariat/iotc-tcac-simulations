# Appendix 2: Description of the Historical Catch Dataset {#catchFields}

```{r}
block_caption("Description of the fields included in the historical catch dataset (1950-2024) considered for the simulations", 
              style = "Table Caption", 
              autonum = run_autonum(seq_id = 'tab', 
                                    bkm = "App2", 
#                                    start_at = 1, 
                                    pre_label = "Tab. A", 
                                    prop = fp_text_lite(bold = TRUE))
)

catch_metadata = data.table::data.table(openxlsx::read.xlsx("../inputs/data/metadata_configuration_file.xlsx", sheet = "catch", sep.names = " "))

catch_metadata[`Field Name` == "CATCH_MT", `Example / Format` := prettyNum(`Example / Format`, big.mark = ",")]

catch_metadata_ft = 
  catch_metadata %>%
  flextable() %>%
  compose(
    j = "Code List",
    value = as_paragraph(
      hyperlink_text(
        x   = `Code List`,
        url = `Code List Url`, 
        props = fp_text(
            color = "blue",
            underlined = TRUE
        )
      )
    )
  ) %>%
  delete_columns(j = "Code List Url") %>% 
  flextable::align(part = "header", align = "center") %>%
  flextable::bold(part = "header") %>%
  flextable::bg(part = "header", bg = "gray") %>%
  flextable::border_inner(border = fp_border()) %>%
  flextable::border_outer(border = fp_border(width = 2)) %>% 
  flextable::width(width = c(1.8, 3, 1.5, 1.1))

catch_metadata_ft
```
